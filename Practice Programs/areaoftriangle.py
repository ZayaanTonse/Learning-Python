# Program to calculate the area of a triangle

baselength=float(input("Enter the base of triangle(inunits):"))
heightlength =float(input("Enter the height of triangle(in units):"))

#Aera formula:0.5*baselength* heightlength
area=0.5*baselength* heightlength

print("The area of triangle is:",area,"Square units")
